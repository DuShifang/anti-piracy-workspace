# 验收准则｜ACCEPTANCE_CRITERIA.md

版本：v0.9.1。适用对象是“本次已承诺和已实现的能力”，不是所有可能功能。本文件是主准则的验收展开，不指定语言、界面、数据库或测试框架。不应为完成清单而新增无关模块，也不能把尚未实现的核心需求写成“不适用”。

## 1. 开始前明确要证明什么

把用户核心任务与验收动作对应起来，可写在现有任务清单或维护说明中。优先验证决定产品价值的高风险环节，随后覆盖完整核心流程及相关失败路径。做完每次小改动运行相关检查，交付前再回归受影响部分；历史测试数量不是当前正确性的证明。

## 2. 所有交付都需要的证据

从用户实际使用的入口启动并完成一个真实操作流程，不以开发命令能跑替代双击、扩展按钮或其他实际入口。已有项目要保留用户数据和不相关功能；需要构建时核对用户启动的确实是新产物。环境不允许验证某入口时明确列出缺口，不能写成已测。

检查界面说明、README 和返回结果是否与实际能力一致。分别报告实际通过、失败、跳过、模拟测试和未验证；HTTP 成功、按钮存在、Agent 自报成功都不等于业务完成。安全、授权和未外发数据的边界不能仅以文案存在证明，要有相关机器检查或受控测试。

## 3. 按功能适用的检查

| 本次包含的能力 | 必须证明的结果，而非指定实现 |
| --- | --- |
| 图形界面 | 真实操作主要页面和二级界面，验证打开、使用、返回/关闭/取消及再次进入；取消不误保存、不误提交，主界面仍可用。没有弹层的产品无需创建弹层。 |
| 搜索与采集 | 有能实际获取线索的路径；来源、检索范围和未执行部分可辨。正常零结果与未知结果分开，遇阻不继续规避。多结果、翻页或跳转不得串值。 |
| 候选复核 | 用相关和不相关样本检查作品对应、误报与漏报风险、人工纠偏；不能只验证“正确项出现”，还要验证“不应发生的项没有发生”。分数或优先级不是侵权结论。 |
| 路由与多渠道处理 | 按实际对象及发现来源选择接收产品；不能因同厂商或相似域名投错渠道。实现多渠道时，各任务的对象、材料、来源和处理状态不互相覆盖。 |
| 网页填表与附件交接 | 复用指定成品与协议；当前软件的对象、材料 role/disclosure、批次、revision 和结果对应正确，不省略扩展自身授权。当前成品的真实最终提交/邮件发送由本人处理，不把字段或附件完成当受理。 |
| 本地数据、导出与升级 | 保存后重开仍正确，导出/备份与恢复范围讲清；修改、清理、回滚不丢用户数据，不把敏感材料夹入源码或诊断。需要何种存储由项目决定。 |
| AI 维修或共用扩展桥接 | 重新打开能定位本项目及对应版本的成品；数据映射和调用失败应可解释，配对切换/取消不串任务，事件持久化和去重符合契约；没有填表需求不强建桥接。共享源码不因某个作者任务被改写。 |

对交互进行测试时，直接调用 API 只能证明相应 API 路径。浏览器 UI、原生 GUI 或扩展界面要用实际支持的交互手段测试；无法做到就标明，不把 API E2E 改名为 UI E2E。普通退出无需固定为右上角 ×、Esc 或遮罩点击；正在提交等不可逆操作应说明关闭界面是否影响后台，不承诺不可能的撤销。

## 本版共用组件的验收分工

作者的软件仍需检查其自身代码、界面、数据、运行入口及任务映射。需要演示交接时使用随包本地模拟资料，并准确区分有没有运行；不让虚构任务进入真实渠道。这里不把“重跑扩展全部测试、实测全部官网”设为个人软件交付前置条件，也不因此要求改扩展源代码。用户已安排真实填表在使用中验证。

接入报告分别写“按发布契约接入”“本作者软件实际完成的检查”“扩展发布包原有验证记录”“待用户实际使用验证”。不要抄发布报告中的测试数当作本轮执行结果。旧协议 0.1、扩展 0.1.0 的参数不得混入 0.2 接入；HTTP 能力声明不是主软件赋予扩展的新渠道或提交权限。

## 4. 真实与模拟各自证明什么

模拟页和虚构数据适合检查字段、材料映射、异常与授权门；少量真实公开来源适合检查当前适配与实际内容特征。两者分开记录，不能互相代替。真实网站中的登录、填写、保存草稿和附件选择可能已有外部效果：开发测试优先在本地受控页面做，不向真实官网发送虚构投诉，不借“还没点提交”绕过材料授权。

需本人或真实材料才能继续时，准确说明停在哪一步，保留已完成能力和后续验证方法。首次合法真实试用可以在明确待验证状态、用户充分知情且逐步授权下进行；没有证据时不抬高声明。一次真实成功只支持相应产品、流程、版本和当时条件，不构成永久保证。

## 5. 交付记录与阻断条件

简要留下：对应需求、验证动作/命令、环境、结果及可查证据位置；不要求固定枚举或复杂报告。已选核心功能未完成时列为缺口；安全授权失败、真实资料误外发、破坏数据、把失败写成成功等问题应阻止相关能力被宣布可用。其他功能可以按真实范围交付，不把局部阻塞变成全项目伪成功或全项目放弃。

案例入口见 [经验索引](LESSONS_FOR_AGENT.md)；错误 23 的共同目标是“声明不超过证据”，错误 24 是“用户流程能安全退出”。历史建议的字段名、Modal 组件或测试技术都不是本准则的要求。

## 发布资料与来源范围

如本次交付复制或改编提示词资料，按 [署名说明](../ATTRIBUTION.md) 和对应许可保留来源；仅用提示词独立生成的软件不强制增加提示词署名或继承其许可。实际复用的扩展、代码和第三方依赖按各自许可交付。隐私说明应对应实际生成软件的行为，不能直接把本资料仓库的 [隐私说明](../PRIVACY.md) 当成软件政策。
