# 工作区变更记录

## v0.9.4｜同步扩展 0.3.1 与公开仓库说明

- 基于 v0.9.3，保留 v0.9.2 的搜索入口参考以及业务、模板、维修与历史案例资料，不退回早期提示词。
- 将当前组件说明和部署示例同步至扩展 0.3.1；传输协议仍为 0.2，HTTP extensionVersion 仍为 0.2.0。
- 更新两份完整接入文档至扩展仓库 e3d0cbb，修正搜索参考、官方渠道索引等仍指向 0.2.0 的断链。
- 区分提示词、源码、本机桥接、商店扩展与已构建 ZIP。补充源码需要构建、商店安装不包含桥接、实际 ID 配对和正式/演示包边界，不把审核中写成已上架。
- 同步 scripting/storage 权限、默认简体中文，以及百度普通下一步、Bing 文件选择与附件状态说明；不更改任务字段、分批限额或授权校验。
- 新增面向使用者的 README、署名、资料许可、隐私和第三方说明；提示词许可不自动约束独立生成的软件，共用扩展另守自身许可。
- 将历史案例中的私人电脑用户名替换为示例名，保留全部问题编号和勘误。旧目录中的其他许可草案未混入。
- 本轮核对版本差异、完整性、链接和文档一致性，未生成个人软件、运行搜索或提交真实投诉，没有新增官网实测结论。

以下章节为历史记录，其中旧版本与当时的授权/能力安排不覆盖当前入口文档。

## v0.9.3｜接入共用填表核心重写版0.3.0

- 共用扩展内部改为平台函数与新的页面执行层，保留12组、34步、14渠道；原动作对应及明确差异见扩展包报告。
- 个人软件继续复用桥接，HTTP协议0.2及extensionVersion=0.2.0不变；安装和部署目录为0.3.0。
- 同步接入和渠道文档，搜索规则、业务资料和历史失败案例不重写。历史章节中的旧版本仍表示当时状态。
- 共享扩展已完成30项本地测试、15项浏览器模拟记录及5项生产核心离线浏览器记录；个人软件生成者不能把这些记为自己的测试结果。没有真实平台投诉实测。


## v0.9.2｜增加搜索采集技术参考，保留当次 AI 的实现选择

- 新增 `references/SEARCH_RULES_REFERENCE.md`，说明九项引擎入口与五个自定义来源，以及特殊卡片、链接取得、翻页、搜索现场、域名与偏好信息；区分采集线索、待明确的执行行为和实施建议。
- AGENTS 增加按任务读取入口。当前作者需要哪些来源、采用何种技术、如何组织代码仍由当次 AI 决定；不要求全部启用，不强制统一搜索执行器，也不能静默删除作者明确需要的能力。
- 工作区提供搜索采集技术说明，不附带可执行的搜索规则包、完整选择器表、私人搜索配置或云端接口；不要求普通作者准备额外规则文件才可开工。
- 主开发准则、验收文件和历史失败档案保持不变；共用填表扩展接入说明的字段、限额和行为约定不变。成品扩展仍为 0.2.0、协议仍为 0.2，不扩展其搜索职责。
- 整合包同步新增该参考并更新封包说明；组件文件、部署相对路径和既有布局保留。已有软件只需合并阅读入口和参考，不用重生成，也不因本补丁重装扩展。
- 本轮仅资料整理、文件完整性与路径核对，没有执行搜索规则、扩展、桥接、测试脚本或真实网站流程；不新增搜索覆盖率或站点有效性认证。

## v0.9.1｜接入统一作者维权填表助手 0.2.0（协议 0.2）

依据：用户明确决定填表复用统一成品，以及本次提供的 author-complaint-assistant-v0.2.0.zip 与对应 EXTENSION_INTEGRATION.md。此版不是扩展开发/测试，也不是作者软件已经升级完成。

- 更新 AGENTS、主准则与相关参考：需要网页填表/附件辅助时接入统一成品，不再由每位作者生成扩展或维护同渠道 DOM；不需要填表的用户不强制接入，其他业务与技术选型仍自由。
- 新增 references/SHARED_EXTENSION.md，明确定位、依赖、交接、事件和维修归属；附上协议 0.2 的接入说明与 CHANNEL_CAPABILITIES.md 工作区说明版，避免误用旧 0.1。
- 接入优先复用 bridge/index.mjs 或 bridge/serve.mjs；部署记录为本包新增的本地定位约定，不混入扩展 HTTP 协议。
- 当前成品真实最终提交/邮件发送由本人完成；不自动采集真实回执。主软件不把 not_submitted、字段完成、附件完成或模拟回执误作真实处理结论。
- 旧失败档案仍沿用 references/REAL_APP_FAILURES_V04.md，错误 1—24和原始保留区不改写；历史候选方案不再覆盖用户后续已明确的共用组件决定。
- 另提供含原样扩展的整合目录及封包说明。不含 Node/Harness/自解压 EXE，不安装依赖，不声明已注册浏览器扩展。
- 本轮仅编辑 Markdown、部署记录及封装文件，检查字节/路径/版本标注；未运行扩展、bridge、测试脚本、浏览器或真实网站，没有新增通过率或真实填表证据。


## v0.5｜投诉自动化边界修正

第一次真实 Harness E2E 暴露出：此前“最终投诉必须用户确认”“敏感材料不得无必要上传”等安全措辞，被合理但过度保守地实现成了“软件只生成草稿，用户必须自己去官网手工填写和提交”。

v0.5 明确修正为：

> **人工确认不等于人工操作。**

用户必须决定是否对本次具体对象投诉、向谁投诉、发送哪些事实和材料；但在完成本次确认后，软件可以在渠道技术可靠、平台流程允许的范围内自动打开官方入口、填写表单、读取并上传本地材料以及完成最终提交。

新增原则：

- 真实敏感材料注入第三方页面前就必须展示接收方、材料项目和用途并取得授权；
- 本地维权资料可以用于已授权的官方投诉，但默认不能提供给 Coding AI 或维修工作区；
- 登录、验证码、短信验证、安全验证、电子签名等本人步骤暂停接管，完成后可继续；
- 渠道按 FULL / ASSISTED / DRAFT_ONLY / MANUAL 等能力等级表达，不强求所有渠道一键提交；
- 对象、接收方、事实正文或附件变化后，旧确认失效；
- “表单填好了”“点击过提交”不等于已经获得回执或已经下架；
- `RULES_SPEC.md` 不再无条件禁止自动最终提交，改为 `user-approved` 提交策略。

本次同步修改：

- `AGENTS.md`
- `BUILD_YOUR_OWN_ANTIPIRACY_APP_PROMPT.md`
- `references/ANTIPIRACY_BUSINESS_GUIDE.md`
- `references/COMPLAINT_TEMPLATES.md`
- `references/RULES_SPEC.md`
- `references/AI_REPAIR_GUIDE.md`

## v0.6｜加入第一次真实生成软件失败档案

- 新增 `references/REAL_APP_FAILURES_V04.md`，完整保存 v0.4 实际开发、实测与交付过程中发生的错误。
- `AGENTS.md` 增加按任务读取该档案的入口。
- `LESSONS_FOR_AGENT.md` 新增两条跨项目硬经验：
  - 用户真正会执行的启动/入口路径必须亲自实测；
  - 搜索解析与候选判断应在功能刚跑通时尽早使用真实公开数据验证。
- 原始记录中的 Python、VBScript、BAT、SQLite 等具体实现不升级为统一技术要求，只把错误模式、测试方法和防护原则迁移到未来项目。

## v0.7｜新增官方投诉渠道索引

- 新增 `references/OFFICIAL_CHANNELS.md`。
- 把“官网 / 官方投诉入口 / 官方指引 / 受理范围 / 核对状态 / 核对日期”从业务指南中抽成独立索引。
- `AGENTS.md` 增加按任务读取规则：涉及“去哪投诉、当前入口是否仍有效”时优先查官方渠道索引。
- `ANTIPIRACY_BUSINESS_GUIDE.md` 保留业务路由原则，不再承担全部 URL 维护职责。
- 当前未验证到稳定版权入口的来源（如搜狗、360搜索）明确标为 `UNVERIFIED`，禁止凭历史或猜测地址自动提交。
- 运行时渠道配置仍由具体软件维护；Markdown 不是唯一运行时数据库。

## v0.8｜把第二个真实生成样本继续并入同一失败档案

- 不新增新的 `REAL_APP_FAILURES_*.md`。
- 继续沿用 `references/REAL_APP_FAILURES_V04.md` 的既有路径和 AGENTS 入口；`V04` 作为历史兼容文件名保留。
- 在同一文件中追加 v0.7 外部内测样本发现的问题，包括：
  - 运行时渠道自行扩大官方索引受理范围；
  - `.gov.cn` / `.com.cn` 的错误同站判断；
  - “跳过来源”和“继续”实际同义；
  - 检索词配额静默丢弃已勾选搜索组；
  - 验证码恢复后无条件重新导航；
  - 零结果与解析失败/JS壳/429状态区分不足；
  - 搜索取证与投诉登录共用 profile，以及静默 `--no-sandbox` fallback；
  - Harness cwd 与 Web UI 实际 Workspace 被混为一谈。
- 对能直接复现的问题和仅由代码结构确认的风险做了明确区分，避免把“风险”写成已发生事故。
- 本次主要更新经验库，不强制未来项目采用某个历史样本的 Node.js / Edge / JSON 架构。

## v0.8.1｜补充渠道自动化能力误标问题

- 在既有 `references/REAL_APP_FAILURES_V04.md` 中追加“错误 23”。
- 明确区分：
  - 官方渠道是否已核实；
  - 自动化是否实现；
  - 真实表单是否适配；
  - 真实提交是否验证；
  - 回执是否真实识别。
- 仅存在通用填表器、fixture 测试或 `ASSISTED` 配置，不得对作者宣称该渠道已经验证可自动提交。
- 推荐将渠道验证状态与自动化验证状态拆成两个维度；只有合法真实提交并识别真实回执后，才能标记为 `REAL_SUBMIT_VERIFIED`。
- 独立浏览器扩展、专用渠道 Adapter 或其他实现均可采用，本条不强制统一架构。

## v0.8.2｜补充 UI 真实路径死胡同问题

- 在既有 `references/REAL_APP_FAILURES_V04.md` 中追加“错误 24”。
- 真实用户发现：某二级弹层可以打开，但没有关闭按钮和其他退出方式；与此同时现有大量测试仍全部通过。
- 明确区分：
  - API / 服务端 E2E；
  - 真实浏览器 UI E2E；
  - 人工用户 Smoke。
- GUI 项目交付前，主要二级界面必须真正点击验证“打开 → 使用 → 关闭/返回/取消”完整路径。
- 推荐将“任何非主界面状态至少存在一个明确、可测试的退出路径”作为 UI 基础不变量。
- 不要求未来项目使用某一种前端框架或 Modal 实现。

## v0.9｜设计决策与知识分层重构（试验版，2026-09-13）

本次重构的是提示词与知识包，不是生成软件的统一代码架构。以 v0.8.2 压缩包为只读基线；没有修改任何作者软件、启动 EXE、Node.js 或 Harness 安装。

- 精简 `AGENTS.md` 为任务入口；主准则统一底线、按需设计、实际开发和如实交付，去掉重复具体流程和固定等级示例。
- 新增 `references/ACCEPTANCE_CRITERIA.md`：只验收已选功能，区分 API、UI、目标环境和真实外部验证，不强制框架或模态框。
- 新增 `references/CANDIDATE_SOLUTIONS.md`：扩展、规则、存储、浏览器协作及维修方案为可选，不引入共享必需服务。
- 业务指南保留原主题，删去重复网址表和统一状态机示例；官方索引与模板原文保留，明确原日期、来源陈述和本轮未核验范围。
- `LESSONS_FOR_AGENT.md` 改成按条件查询的风险索引；同一路径 `references/REAL_APP_FAILURES_V04.md` 保留错误 1—24全部原文，前置索引、证据状态与明确勘误。
- `RULES_SPEC.md` 保留原文但标明历史协议和兼容边界；不凭 MD 字段认为执行器已经实现。
- 允许内部 `app/` 等目录；长期项目根入口和实际维修路径仍须明确。普通维护与受限自动维修不强行合并。
- 外部事实可根据新官方依据更新；公共 MD 不是永久上限。单个成功修法不自动升级成全体默认方案。

本轮只做文档重构与静态包检查；没有新的 Harness 生成实验、Windows 启动验证、浏览器扩展验证或真实投诉验证。不同作者适配性是否改善，待独立对照测试；不将本记录当成“软件已经升级成功”。
