# 共用填表扩展接入｜SHARED_EXTENSION.md

版本：v0.9.4。项目维护者已确定网页自动填表/附件辅助采用统一成品；本文件负责选择与定位，不另造协议。对应扩展为 [作者维权填表助手](https://github.com/DuShifang/author-complaint-assistant) 0.3.1。本仓库保存接入文档快照，不包含扩展安装包、完整桥接源码、Node 或模型凭据。共享扩展已有本地与离线浏览器验证记录，未完成真实平台登录后的投诉全流程实测。

## 1. 区分安装包版本与传输协议

完整阅读 [EXTENSION_INTEGRATION.md 工作区说明版](shared-extension-0.3.1/EXTENSION_INTEGRATION.md)，按用户选定渠道阅读 [CHANNEL_CAPABILITIES.md 工作区说明版](shared-extension-0.3.1/CHANNEL_CAPABILITIES.md)。这两份文档同步自扩展仓库 0.3.1；除调整跨仓库链接外保留契约原义，接入时核对对应版本成品契约及其 `shared/protocol.js` 校验实现。文字表述不要求与安装目录中的文档逐字一致，不能以表述调整放宽校验或兼容旧 0.1。

成品名“作者维权填表助手”，安装包版本0.3.1、协议0.2；HTTP中的extensionVersion仍为0.2.0，用于兼容已有提供器的精确比较，旧 0.1 提供器不自动兼容。HTTP 请求/响应、任务、材料用途、事件、授权、限额均按对应版本成品契约实现，不能把下面部署记录里的字段混入 HTTP task。

用户的软件负责需求、搜索、复核、事实/正文、材料保管、交接界面和结果记录；扩展负责它所声明渠道的网页填写、附件辅助和本人接管。无填表需求时无需安装/启动桥接。需要填表时不要再生成另一份扩展、复制平台 DOM 规则、写平行万能填表器或后台提交器；共享缺陷交回共享维护项目。

## 2. 定位成品，不把文档当安装包

优先读取项目根目录 `.runtime/SHARED_COMPONENTS.json`（如果有）。这是本工作区新增的本地部署记录，不是扩展协议的一部分；不得保存 API Key、配对码、身份、Cookie、用户资料目录或真实任务。

记录的 `pathBase: workspace-root` 表示 components 中 `root` 的相对路径，以本项目根目录为基准；组件的 paths 则相对于该 root。绝对 root 可用于启动器统一安装，但必须来自维护者部署或用户明确选定的位置，不能从网页/任务字段指定。验证文件存在与版本后引用；不要全盘扫描私人目录找扩展。

合包示例中，工作区为 `作者软件/`，共用目录为 `shared-components/author-complaint-assistant/0.3.1/`，因此 root 记录为 `../shared-components/author-complaint-assistant/0.3.1`。整体移动合包仍保持关系；仅移动作者软件时需重新定位并更新该记录。未来启动器可以换部署目录，但应写实际路径而不是这个示例的假绝对路径。

本机桥接组件根目录至少应能找到 `package.json`、`package-lock.json`、`bridge/index.mjs`、`bridge/serve.mjs`、`shared/protocol.js`、`shared/channels.js`、两份必读文档及其运行依赖文件。文档副本不能替代完整 bridge/、demo/、shared/ 等运行文件。采用源码加载浏览器扩展时，还需构建生成 `dist/production/manifest.json`；采用商店安装时，浏览器扩展可以独立于本机桥接目录存在，不以本机缺少 dist 判定商店扩展未安装。

没有部署记录或组件不在记录位置时，明确说明缺的是成品/路径/版本，让维护者提供位置；不把它当已安装，也不重新造扩展补空。不得仅凭文件夹名声称浏览器已加载、配对已完成或渠道已实测。

## 3. 准备运行条件与浏览器加载

扩展加载本身不需要 Node；配套桥接需要 Node 22+，在成品根目录按对应版本成品部署说明运行 `npm ci --omit=dev` 准备运行依赖。成品自带的锁文件和依赖声明一起保留。由启动器或 Coding AI 在权限允许时准备，不让普通作者负责安装命令；不要为方便执行更新到 latest、重写 package 文件或改变协议。

本提示词资料包不包含 Node、node_modules、模型工具或扩展安装程序，也不替用户完成安装、配对或模型配置。分别核对实际准备情况，不能把下载资料记为完成部署。

浏览器安装分两种情况：

- 已存在可核实的 Edge 商店上架页时，可从商店安装浏览器扩展；本机桥接仍需由配套软件准备。商店“提交审核”“认证中”和实际上架不是同一状态。本次资料整理未确认正式上架 URL，不编造商店地址或宣称已上线。
- 源码安装时，在扩展源码根目录运行 `npm ci`、`npm run build`，再在浏览器扩展管理页加载生成的 `dist/production`；若持有已经构建的正式 ZIP，则解压后选择含 manifest.json 的目录。GitHub 的 Code → Download ZIP 下载的是源码，不保证含 dist。

`dist/demo` 仅用于独立本地演示，不当作商店正式包；正式包不支持 local.mock.copyright。0.3.1 中文发布构建使用 default_locale=zh_CN，仅申请 scripting、storage 及固定网站/本机回环地址权限，不再申请 activeTab；这些元数据不是传输协议字段。

由作者正常完成浏览器安装授权，读取实际 32 位扩展 ID 后在本机交接页配对；从源码版切换到商店版可能改变 ID，需要重新配对。配对码和令牌不发给模型，不静默安装或跳过浏览器授权。

## 4. 只为个人软件写薄接入

Node 项目优先引用共用根目录的 `bridge/index.mjs` 中 `startAuthorBridge`；其他语言用参数数组启动该根目录下的 `bridge/serve.mjs`，不要拼接带私人数据的 shell 命令。主软件可以使用任何语言或数据库，不把桥接环境变成整款软件的技术栈要求。

按契约从已复核任务生成 task 与 assets，调用共用材料准备及分批组件，保留每批独立对象与链接文件。CLI 清单放在当前作者私有数据位置，相对材料路径按清单所在目录解析；HTTP task、扩展消息、材料授权不能出现本地文件路径。当前任务材料是稳定副本，不读全库、不把合同或身份证传给 Coding AI。

材料 role/category/disclosure、每渠道正文长度与批数按对应版本成品契约处理；不要沿用旧版“一律一份权利材料/不能有身份材料/20个对象”等限制。缺失字段暂停或由本人处理；不得推算保护期限、造签名、自动猜收件人、默默截断正文。QQ 邮件会附上本次非 signature 材料，只交接本收件人应取得的内容。

软件内应有可找到的“交给填表助手”等入口，生成本次任务并显示真实配对页，而不只是官网链接或空按钮。生成配对码、选择批次、扩展本地预览、选择网页和材料授权各自按契约由本人完成；主软件不能代点或用 approved 字段授权。一次一任务/批次，切换按原流程清除后重新配对；不要擅自建设后台全库连续投递。

## 5. 结果必须按原义保存

使用契约要求的 `(eventStreamId, event.seq)` 持久化去重，保存成功才确认回调；不要只用 taskId+seq 跨配对去重。revision 或材料内容/用途变化需重新交接；过期、断线、页面变化或未知结果不自动重放。

当前生产成品的请求只用 fill、upload；mockSubmit 仅限配套本地模拟渠道。真实最终提交、邮件发送、受理号和进度自动采集不属于本版能力。not_submitted 表示扩展未自动提交，不能推出本人未在网页手工提交；MEMBER_STEP_DONE、completed、选中文件或附件控件完成不等于投诉受理。主软件可单独记录作者手工填写的回执，必须标注来源，不能伪装为扩展观察。

`GET /v1/capabilities` 是本机提供器向扩展声明运输与接口能力，不是主软件修改扩展渠道的 API。接入说明第 7 节的 14 个 channelKey 是包内声明的具体流程，不是 14 个独立平台或现场通过率；没有抖音、腾讯文曦、小说搜索采集、自动下架监控等能力。当前需要但未支持的渠道如实保留人工路径，不靠伪造 channelKey 或 allowedHosts 越界。

## 6. 开发、验收与以后维修

本次使用安排是先接入，真实自动填写效果在作者实际使用中验证；不要把重新测试共用扩展的所有渠道作为个人软件开工的先决条件，更不要擅自向官网填虚构事项。作者软件自身的界面、映射、任务记录、数据保存和其他功能仍按验收准则开发检查；确有本地交接演示需按授权范围执行，并写明是否实际运行。

故障先判断责任：正文超长、材料用途错、版本/路径错、事件未落库属个人软件接入；扩展步骤、选择器、附件执行、公共 bridge 行为的缺陷属共用项目。后者收集不含秘密的 channel/code/版本及必要说明，按成品 MAINTENANCE.md 回共享项目维护，不在每位作者目录各自修一遍公共代码。

以成品版本管理共享发布，不默认兼容任意未来版本，不因规则更新而静默升级全部作者。Node 依赖准备不会把“扩展已加载、已配对、真实渠道已通过”一起变成真；分别说明实际状态。


本次共用组件安装包为0.3.1，HTTP配对/能力字段extensionVersion仍使用0.2.0，协议仍为0.2。遵循随包接入文档，不能以安装包版本替换HTTP字段。

## 7. 结果状态与许可的补充

Bing 的 MEMBER_FILE_SELECTED 表示已核对待提交文件选择；其他可观察材料槽的 MEMBER_ATTACHMENT_OBSERVED 表示观察到附件状态。两者都不是平台受理或最终提交回执。百度部分普通“下一步”和链接校验由扩展处理，验证码、扫码及最终提交仍由本人完成；主软件以扩展实际返回事件为准，不另写导航或上传实现。

提示词资料的许可只约束提示词资料，不自动覆盖生成的软件。实际接入、复制或分发共用扩展及桥接代码时，另遵守 [扩展许可快照](shared-extension-0.3.1/LICENSE) 并核对扩展仓库的对应版本条款。作者维护自己作品可免费使用；收费分发、商业集成或收费代维权等用途须按扩展许可事先取得授权。不能因为生成的软件不继承提示词许可，就忽略实际复制组件的版权、材料权限和许可条件。