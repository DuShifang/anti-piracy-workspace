# 候选实现｜CANDIDATE_SOLUTIONS.md

版本：v0.9.1。以下大部分仍是候选实现；第 3 节网页填表已由项目维护者明确选择共用成品，不再要求每位作者重新选型。本轮没有新增工程验证。只在存在相应问题时阅读；采用历史成功方案不算失去个性，采用新方案也不自动更好。

## 1. 易变的网站适配

少量来源可以用普通模块或脚本，把站点相关逻辑限制在容易找到的地方。来源和交互较多时，可比较规则配置、专用适配器或其他模块化方式；[RULES_SPEC.md](RULES_SPEC.md)保留一种历史设计。取舍看当前修改成本、表达复杂度与测试难度，不看是否“用了完整 DSL”。不支持的配置要明确失败，不为跑通样例静默忽略关键字段。

## 2. 自动搜索与人工协作

可比较当前允许且可用的接口/请求采集、可见浏览器辅助、用户触发采集和手动导入。选择由用户习惯、真实来源响应及限制决定；不把固定来源或 AUTO 等枚举写成必需。遇到登录、验证或限流时按主准则接管/退让，普通浏览器模式也不是绕过限制的保证。降级要保留可理解的任务状态，不能把全部机械操作无说明地丢回用户。

## 3. 网页填表：已确定接入共用成品

需要网页填表或附件辅助时，使用“作者维权填表助手 0.3.1／协议 0.2”，按 [SHARED_EXTENSION.md](SHARED_EXTENSION.md)查成品位置、完整契约和渠道资料。这是用户明确选择的公共能力，不是根据某个样本擅自推广的默认架构。只有不需要填表的作者可以不接入；不能因此要求所有人使用同一套个人软件。

作者软件自行决定任务界面、存储、材料管理与其他业务。桥接优先引用共用目录的 bridge/index.mjs，其他语言调用 bridge/serve.mjs；不需要把整款软件改为 Node。不要复制 extension/、dist/ 或 shared/platforms/ 中的平台 DOM 适配到个人项目，不再造一个平行通用填表器。只导出材料的用户仍可只导出；未支持的渠道可提供准确的人工路径，不伪造新渠道。

组件路径、依赖和协议不满足时说明原因并按部署说明准备，不改源码来绕过校验。统一维护不等于强制云服务或静默更新；版本升级需要明确兼容与用户安排。现有项目按任务范围逐步切换，保留原数据和记录，避免两套执行路径同时处理一份投诉。

## 4. 数据与浏览器环境

文件、JSON、本地数据库或其他存储按规模和可靠性选择，不把历史 SQLite 当必选。敏感资料默认不放进源码与维修材料；需要本地档案时允许管理和删除，备份范围说清。隔离源码与数据、分浏览器配置、临时脱敏样本是可比较的办法，不是安全证明的替代品。便携模式需与作者确认相应风险，不用固定路径替代需求判断。

## 5. 维护形态

小项目可以用正常的项目维护入口加简短说明；有需要时再做脱敏诊断导出；局部问题可可靠独立验证时才评估自动候选维修。每种方式都应修到作者实际启动的产物。更完整的参考见 [AI_REPAIR_GUIDE.md](AI_REPAIR_GUIDE.md)，不要求所有软件具有同一种 Harness 适配类、候选文件或目录。

## 6. 选择与放弃同样需要理由

只记录与当前用户有关的关键取舍即可，不强制长篇评审。核心需求暂做不到时说明未完成及原因；不要为了避免复杂度把用户明确要的填写能力替换成官网链接，也不要为了复用组件加入她明确不需要的投诉模块。


本次共用组件安装包为0.3.1，HTTP配对/能力字段extensionVersion仍使用0.2.0，协议仍为0.2。遵循随包接入文档，不能以安装包版本替换HTTP字段。
